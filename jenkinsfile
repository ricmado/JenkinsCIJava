#!/usr/bin/env groovy


pipeline {
  agent any

  stages {
    stage('build') {
      steps {
		sh 'mvn -f ./my-app/pom.xml package'
		}
    }
    stage('run') {
      steps {
        sh 'java HelloWorld'
		sh 'java -cp target/my-app-1.0-SNAPSHOT.jar com.mycompany.app.App'
      }
    }
  }
}